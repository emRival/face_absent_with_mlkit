name: students_attandance_with_mlkit
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.1
  camera: ^0.11.1
  geolocator: ^13.0.2
  intl: ^0.20.1
  lottie: ^3.3.1
  dotted_border: ^2.1.0
  google_mlkit_face_detection: ^0.12.0
  cloud_firestore: ^5.6.2
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/raw/
